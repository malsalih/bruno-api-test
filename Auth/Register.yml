info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/register?name=mohammed&email=user3@example.com&password=password&role=provider"
  params:
    - name: name
      value: mohammed
      type: query
    - name: email
      value: user3@example.com
      type: query
    - name: password
      value: password
      type: query
    - name: role
      value: provider
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
